function renderShowNotFound() {
  let notFoundHtml = "";
  notFoundHtml += `<h2 class="title3">Lo sentimos, no se ha encontrado ninguna serie</h2>`;
  notFoundHtml += `<i class="far fa-frown"></i>`;
  showNotFound.innerHTML = notFoundHtml;
}
